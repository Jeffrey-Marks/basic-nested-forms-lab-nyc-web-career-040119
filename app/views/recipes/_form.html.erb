<%= form_for @recipe do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.fields_for :ingredients do |ing| %>
    <%= ing.text_field :name %>
    <%= ing.number_field :quantity %>
  <% end %>

  <%= f.submit %>
<% end %>
